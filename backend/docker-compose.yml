services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    image: speakup-backend

    ports:
      - "3002:3002"

    env_file:
      - .env

    restart: unless-stopped